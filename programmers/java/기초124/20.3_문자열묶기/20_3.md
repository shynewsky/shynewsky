# ⭐ ++cnt[len]

- 배열 원소에 대한 전위 증가(pre-increment)

```java
int[] cnt = new int[31];
// cnt : int 배열
// cnt[len] : 길이가 len인 문자열의 개수
// 배열은 기본값이 전부 0
```
```java
++cnt[len];
// cnt[len] = cnt[len] + 1;
```

