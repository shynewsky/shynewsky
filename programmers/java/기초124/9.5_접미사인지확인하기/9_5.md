# ⭐ endsWith()

- 문자열이 특정 문자열로 끝나는지를 검사하는 String 클래스의 메서드

```java
boolean result = 문자열.endsWith(접미사);
```
```java
String s = "banana";

s.endsWith("ana");   // true
s.endsWith("na");    // true
s.endsWith("ban");   // false
s.endsWith("banana"); // true
s.endsWith("abanana"); // false
```