# 누적합







---

# 실수모음
   
1. 문자열 끝에 뭐가 붙어 있는지
```python
S = input() # X
S = input().strip() # O
```

2. r번째 문자를 포함해서 생각한다
```python
[r+1 까지 누적] - [l 까지 누적] # X
[r까지 누적] − [(l−1)까지 누적] # O
```

3. 문자는 정수 인덱스로 바로 변환 가능
```python
[arr.index(a)] # X
[ord(a) - 97]  # O
```
