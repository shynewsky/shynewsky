# 순열
```python
    for i in range(1, N1):
        if visited[i] == 1:
            continue
        visited[i] = 1
        path.append(i)
        f(cnt+1)
        path.pop()
        visited[i] = 0
```

# 조합
```python
    for i in range(start, N1):
        if visited[i] == 1:
            break
        visited[i] = 1
        path.append(i)
        f(cnt+1, i+1)
        path.pop()
        visited[i] = 0
```

# 중복순열
```python
    for i in range(1, N1):
        path.append(i)
        f(cnt+1)
        path.pop()
```

# 중복조합
```python
    for i in range(start, N1):
        path.append(i)
        f(cnt+1, i)
        path.pop()
```


